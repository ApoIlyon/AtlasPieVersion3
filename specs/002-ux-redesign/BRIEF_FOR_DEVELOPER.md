# Бриф: Переделка интерфейса AutoHotPie

## Проблема

Текущий интерфейс **непонятный и непрофессиональный**:
- ❌ Радиальное меню кривое (простые кнопки вместо красивых сегментов)
- ❌ Профили не работают правильно (баги, нет визуального редактора)
- ❌ **Нет возможности добавить свои команды** - это критично!

## Задача

Взять **дизайн** из `kando-2.1.0-beta.1-main` и **функции** из `AutoHotPiehttps-main`, реализовать всё это в нашем Tauri-приложении.

## Что делать

### 1. РАДИАЛЬНОЕ МЕНЮ КАК В KANDO (Приоритет: КРИТИЧЕСКИЙ)

**Референс:** `kando-2.1.0-beta.1-main/src/menu-renderer/`

**Что нужно:**
- Радиальные сегменты (wedge shape), а не простые кнопки
- Плавные spring-анимации открытия/закрытия
- Центральное ядро с текстом выбранного элемента
- Hover эффект: увеличение сегмента (scale 1.15x) + glow
- Визуальные разделители между сегментами
- Выбранный путь подсвечивается

**Технически:**
- SVG для сегментов + Framer Motion для анимаций
- 60 FPS обязательно
- Radius: 156px, gap: 4°

**Файлы для создания:**
```
src/components/pie/
  ├── RadialPieMenu.tsx          (главный компонент)
  ├── PieSegment.tsx             (радиальный сегмент)
  ├── CenterCore.tsx             (центр с текстом)
  ├── WedgeSeparator.tsx         (разделители)
  └── SelectionPath.tsx          (подсветка пути)

src/utils/
  └── radial-math.ts             (математика: polarToCartesian, getWedgePath)
```

**Цвета (Dark theme):**
```css
--color-bg: #111111
--color-accent: #35B1FF
--color-text-primary: #ffffff
--color-text-secondary: #a0a0a0
```

### 2. ВИЗУАЛЬНЫЙ РЕДАКТОР ПРОФИЛЕЙ (Приоритет: ВЫСОКИЙ)

**Референс:** `AutoHotPiehttps-main/src/editPieMenu.js`

**Что нужно:**
- Canvas с живым предпросмотром меню
- Drag-and-drop для перестановки сегментов
- Breadcrumb-навигация по вложенным уровням
- Undo/Redo (Ctrl+Z / Ctrl+Shift+Z)
- Валидация: максимум 12 сегментов, 3 уровня глубины

**Технически:**
- `@dnd-kit/core` для drag-and-drop
- Zustand + Immer для state management
- History API для undo/redo

**Файлы для создания:**
```
src/components/editor/
  ├── VisualMenuEditor.tsx       (главный редактор)
  ├── SegmentDragDrop.tsx        (drag-and-drop)
  ├── MenuBreadcrumb.tsx         (навигация)
  ├── LivePreview.tsx            (предпросмотр)
  └── EditorHistory.tsx          (undo/redo UI)

src/state/
  └── editorStore.ts             (state с историей)
```

### 3. СИСТЕМА ПОЛЬЗОВАТЕЛЬСКИХ КОМАНД (Приоритет: КРИТИЧЕСКИЙ!)

**Референс:** `AutoHotPiehttps-main/src/createNewScript.js`, `functionSelect.js`

**Что нужно:**
Библиотека команд где пользователь может:
- Создавать свои макросы (последовательность клавиш)
- Сохранять mouse actions (клики, движения)
- Писать кастомные скрипты
- Использовать шаблоны (templates)
- Тестировать команды перед сохранением

**Типы команд:**
1. **Keyboard Macro** - последовательность клавиш с задержками
2. **Mouse Action** - клики, движение мыши
3. **System Command** - открыть файл, URL, приложение
4. **Composite** - цепочка из нескольких команд
5. **Conditional** - с условиями (if/then)
6. **Custom Script** - пользовательский код

**Технически:**
- Backend (Rust): `enigo` crate для симуляции клавиатуры/мыши
- Frontend: Monaco Editor или CodeMirror для редактора
- Validation с помощью Zod

**Файлы для создания:**

Backend:
```
src-tauri/src/
  commands/
    └── custom_commands.rs      (API: create, update, delete, execute)
  services/
    ├── macro_engine.rs         (выполнение макросов)
    └── command_library.rs      (хранение команд)
  domain/
    ├── custom_command.rs       (модель данных)
    └── command_template.rs     (шаблоны)
```

Frontend:
```
src/components/commands/
  ├── CommandLibrary.tsx        (библиотека команд)
  ├── MacroEditor.tsx           (редактор макросов)
  ├── CommandBuilder.tsx        (конструктор команд)
  ├── CommandTester.tsx         (тестирование)
  └── TemplateSelector.tsx      (выбор шаблонов)
```

**Пример структуры команды:**
```rust
struct CustomCommand {
    id: Uuid,
    name: String,
    command_type: CommandType,  // Keyboard, Mouse, System, etc.
    steps: Vec<CommandStep>,    // Шаги выполнения
    tags: Vec<String>,          // Для поиска
}

struct CommandStep {
    action: ActionPayload,      // Что делать
    delay_ms: Option<u32>,      // Задержка после
    condition: Option<Condition>, // Условие
}
```

### 4. FIX ПРОФИЛЕЙ (Приоритет: ВЫСОКИЙ)

**Проблемы сейчас:**
- Профили не активируются правильно
- Хоткеи конфликтуют
- Нет debug-режима

**Что сделать:**
- **ProfileDebugger** - показывает почему профиль не сработал
- **ConflictResolver** - решает конфликты хоткеев автоматически
- Расширенное логирование всех событий
- Auto-recovery при ошибках

**Файлы для создания:**
```
src/components/profile/
  ├── ProfileDebugger.tsx       (UI для отладки)
  ├── ConflictResolver.tsx      (решение конфликтов)
  └── ProfileValidator.tsx      (валидация)

src-tauri/src/services/
  └── profile_debugger.rs       (backend для debug)
```

## План реализации

### Этап 1: Радиальное меню (3 дня)
1. Создать компоненты SVG-меню
2. Добавить анимации
3. Интегрировать вместо старого PieMenu

### Этап 2: Визуальный редактор (4 дня)
1. Drag-and-drop редактор
2. Live preview
3. Undo/Redo
4. Интеграция с ProfileEditor

### Этап 3: Пользовательские команды (5 дней)
1. Backend: Rust API для команд
2. Macro engine (выполнение)
3. Frontend: библиотека команд
4. Редактор макросов
5. Templates

### Этап 4: Fix профилей (3 дня)
1. Debug tools
2. Conflict resolution
3. Улучшенная валидация

### Этап 5: Тесты и документация (3 дня)
1. E2E тесты
2. Performance тесты
3. Migration guide

**Итого: ~18 дней**

## Зависимости для установки

```json
{
  "@dnd-kit/core": "^6.1.0",
  "@dnd-kit/sortable": "^8.0.0",
  "immer": "^10.0.3",
  "zod": "^3.22.4"
}
```

```toml
# Cargo.toml
[dependencies]
enigo = "0.2"      # Keyboard/mouse simulation
serde_yaml = "0.9" # Templates
regex = "1.10"     # Validation
```

## Критерии приемки

### Радиальное меню:
- ✅ Выглядит как в Kando (красивые сегменты)
- ✅ 60 FPS при анимациях
- ✅ Hover эффект работает
- ✅ Центральный текст показывает выбранный элемент

### Редактор профилей:
- ✅ Можно перетаскивать сегменты
- ✅ Undo/Redo работает без багов
- ✅ Live preview обновляется в реальном времени
- ✅ Валидация предотвращает ошибки

### Пользовательские команды:
- ✅ Можно создать keyboard macro
- ✅ Можно создать mouse action
- ✅ Можно протестировать команду
- ✅ Templates применяются
- ✅ Команды сохраняются и загружаются

### Стабильность:
- ✅ Профили активируются правильно
- ✅ Конфликты хоткеев обнаруживаются
- ✅ Debug mode показывает проблемы
- ✅ 0 критических багов

## Референсы

**Kando (дизайн):**
- `kando-2.1.0-beta.1-main/src/menu-renderer/menu.ts` - главная логика
- `kando-2.1.0-beta.1-main/src/menu-renderer/selection-wedges.ts` - визуализация
- `kando-2.1.0-beta.1-main/src/menu-renderer/menu-theme.ts` - темы

**AutoHotPie (функции):**
- `AutoHotPiehttps-main/src/editPieMenu.js` - редактор
- `AutoHotPiehttps-main/src/createNewScript.js` - скрипты
- `AutoHotPiehttps-main/src/functionSelect.js` - выбор функций
- `AutoHotPiehttps-main/src/classes/ahpClasses.js` - классы данных

## Документация

Полная документация в:
- `specs/002-ux-redesign/spec.md` - детальная спецификация
- `specs/002-ux-redesign/plan.md` - подробный план (52 задачи)
- `specs/002-ux-redesign/design-reference.md` - визуальный референс с кодом

## Вопросы?

Если что-то непонятно - посмотри референсные проекты или документацию в `specs/002-ux-redesign/`.

---

**Самое важное:**
1. Радиальное меню должно быть **красивое как в Kando**
2. Редактор должен быть **удобный с drag-and-drop**
3. **ОБЯЗАТЕЛЬНО** добавить возможность создавать свои команды/макросы

Без этих трех вещей задача не считается выполненной! 🎯
